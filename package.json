{
	"name": "obsidian-patch-select",
	"version": "0.1.0",
	"description": "Obsidian plugin that sends MIDI program changes from frontmatter.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production",
		"install:local": "node scripts/install.mjs",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"midi"
	],
	"author": "Matthew",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.16.5",
		"builtin-modules": "^4.0.0",
		"esbuild": "^0.24.0",
		"obsidian": "^1.6.7",
		"tslib": "^2.7.0",
		"typescript": "^5.6.2"
	}
}
